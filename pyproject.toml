[project]
name = "natpierce-qiandao"
version = "0.1.0"
description = "自动签到脚本 - 使用 Playwright 和 ddddocr 实现"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "playwright>=1.40.0",
    "ddddocr>=1.4.11",
    "pillow>=10.0.0",
    "numpy>=1.24.0",
]
